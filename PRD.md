# GO. (고점) — Product Requirements Document

| 항목 | 내용 |
| --- | --- |
| 프로젝트명 | GO. (고점) |
| 문서 버전 | v1.0 |
| 최종 수정일 | 2026-08-13 |
| 소속 | 멋쟁이사자처럼 대학 14기 중앙 해커톤 · 주제기업 AAC(Anti-Aging Club) · 팀 뚝딱이들 |
| 플랫폼 | Mobile Web (Mobile First) |
| 문서 목적 | 기획·디자인·프론트·백엔드·AI가 공유하는 **개발 기준 문서**. 본 문서와 유저플로우 다이어그램이 충돌할 경우 본 문서를 우선한다. |

---

## 1. 제품 개요

### 1.1 한 줄 정의

> **GO.** 은 AI가 사용자의 현재 얼굴과 신체정보를 분석하고, 가장 현실적으로 도달 가능한 **추구미**와 변화 전략을 제안하여 사용자의 **고점(Highest Potential)** 으로 나아가도록 돕는 AI 기반 이미지 전략 서비스다.

### 1.2 목표

입력한 내 얼굴과 추구미(또는 추구하는 생활·얼굴형·체형·건강)를 바탕으로 AI가 비교 분석한 뒤,
나에게 맞는 추구미와 **현실적인 변화 전략 + 지속 가능한 루틴**을 제안한다.

### 1.3 배경

**① 웰니스 측면 — 수치 관리의 피로**

웰니스는 신체적·정신적·사회적 건강의 균형을 추구하는 활동이다. 그러나 웰니스 실패 요인으로 *"건강을 점수로 기록하고 수치로 관리하는 방식에 대한 피로"* 가 부상하고 있다. 수치는 분석·관리에는 탁월하지만 인간에게 피로감을 주고, 불특정 다수의 빅데이터로 만들어진 **보편화된 기준은 나에게 맞지 않을 수 있다.**

→ GO.는 보편 수치가 아닌 **'추구미(이상적인 나)'** 를 목표로 세워, 오직 나만을 위한 기준으로 일상을 바꾼다.

**② 미용 측면 — 채널 분산으로 인한 소비자 혼란**

> 김경인. (2021). 멀티채널환경의 정보탐색과 소비자 혼란에 대한 연구 - 뷰티서비스를 중심으로 -. 한국디자인포럼, 26(1), 147-156.

뷰티 정보 채널(온라인/오프라인/SNS/후기/병원/광고)이 과도하게 분산되어 소비자는 **Consumer Confusion**을 겪는다. 사용자는 하나의 원하는 이미지를 만들기 위해 인스타그램·유튜브·올리브영 리뷰·병원 후기·커뮤니티를 반복 탐색하지만, 신뢰 수준이 달라 무엇이 자신에게 맞는지 판단하기 어렵다.

→ GO.는 **하나의 이미지(추구미)를 중심으로 모든 솔루션을 연결한다.**

### 1.4 문제 정의와 해결

| 구분 | 내용 |
| --- | --- |
| 사용자 문제 | SNS에서 관리·스타일링 정보를 자주 접하고 원하는 분위기의 레퍼런스를 저장하지만, 자신의 얼굴·체형·생활루틴에 **무엇부터 적용해야 할지 몰라** 탐색과 결정을 반복한다. |
| 해결 방안 | 사용자의 정보(사진·신체·건강정보)와 사용자가 등록한 원하는 분위기를 AI가 비교 분석해, **현실적으로 도달 가능한 추구미와 변화 전략**을 제안하는 플랫폼. |
| 핵심 가치 | "원하는 이미지에 **가까워지는 방법**"을 얻는 것. |
| 차별점 | 추구미(사진/텍스트)와 사용자 정보(사진·신체·건강·생활패턴)를 **상세 비교 분석**하여 적용 가능한 방안을 제안하고, **변화된 모습을 시각적으로 보여주며**, 이를 **루틴으로 연결**한다. |

### 1.5 서비스 원칙 (Design Principle · AI 가드레일의 근거)

> 외모를 점수화하거나 타인의 얼굴을 복제하지 않는다.

| 원칙 | 의미 |
| --- | --- |
| 평가보다 해석 | 결점이 아닌 분위기 구성 요소를 설명한다. |
| 복제보다 적용 | 참고 이미지의 스타일만 나에게 맞게 적용한다. |
| 이미지보다 행동 | 실제로 시도할 수 있는 변화 순서를 제안한다. |
| 사용자 선택 우선 | AI 추천보다 사용자의 최종 선택을 우선한다. |

---

## 2. 타겟 사용자

### 2.1 타겟

SNS에서 관리·스타일링 정보를 자주 접하고, 원하는 분위기의 레퍼런스를 저장하지만 자신의 얼굴·체형·생활루틴에 무엇부터 적용해야 할지 몰라 탐색과 결정을 반복하는 **20~30대 사용자**.

### 2.2 사용자 시나리오 (Happy Path)

1. 사용자가 자신의 **상반신 이미지**와 **체중·키**를 필수로 등록하고, **평균 수면 시간·인바디 측정값**을 선택 입력한다.
2. **피부 / 얼굴형 / 체형 / 건강** 중 더 중요하게 생각하는 **우선순위 1개**를 선택한다. (건너뛰면 동일 가중치)
3. 추구미와 관련된 **자연어**, 또는 **자연어 + 사진**을 등록한다.
4. AI가 사용자 정보와 비교 분석해 **키워드 후보**를 추출하고, 사용자가 **1개 이상 선택**한다.
5. **추구미 결과 화면**(예상 이미지 · 변화 설명 · 관리 방향)을 확인한다.
6. 마음에 들면 결과를 **서랍(즐겨찾기)에 저장**하고 갤러리에 이미지를 내려받는다.
7. 메인 화면에서 **루틴 생성**을 누르면 AI가 분석 데이터를 바탕으로 도달 가능한 루틴을 계획하고, **시간에 맞춰 알림**을 보낸다.

---

## 3. 성공 지표 (MVP 기준)

| 구분 | 지표 | 목표 |
| --- | --- | --- |
| 핵심 전환 | 프로필 등록 → 추구미 분석 완료율 | 60% 이상 |
| 결과 만족 | 결과 화면에서 "마음에 들어요" 선택 비율 | 70% 이상 |
| 연결 전환 | 분석 완료 → 루틴 생성 전환율 | 40% 이상 |
| 지속성 | 루틴 생성 후 7일 내 완료 체크 1회 이상 | 50% 이상 |
| 품질 | 분석 실패(에러·타임아웃) 비율 | 3% 이하 |
| 성능 | 분석 요청 → 결과 노출 P95 | 30초 이내 |

---

## 4. 범위 (Scope)

### 4.1 MVP 포함 (In Scope)

- 로그인 / 회원가입
- 내 사진 + 기본 정보 등록 → 현재 프로필 생성
- 우선순위 선택(선택 사항)
- 추구미 입력 (자연어 / 자연어+사진)
- AI 키워드 추출 및 사용자 선택
- 추구미 결과 생성 (예상 이미지 · 변화 Top 3 · 관리 방향)
- 결과 저장(서랍/즐겨찾기) 및 이미지 다운로드
- 카테고리·기간 기반 루틴 생성
- 루틴 완료 체크 및 미수행 루틴 유동 재배치
- 루틴 알림
- 무료 체험 / 분석권 / 구독 결제 게이팅

### 4.2 MVP 제외 (Out of Scope)

- 소셜/커뮤니티, 친구 공유 피드
- 제품·시술 커머스 연동 및 예약
- 웨어러블·헬스 플랫폼 자동 연동
- 다국어 지원
- 웹 데스크톱 전용 레이아웃 (반응형 대응만)

---

## 5. 유저플로우

첨부된 최종 유저플로우 다이어그램(`GO. 최종 사용자 흐름`)을 정본으로 한다.

```text
[01 · 시작과 현재 프로필]
GO. 서비스 진입 → 로그인 → 메인 화면
  → 내 사진 · 기본 정보 등록
     (생년월일 · 성별 · 키 · 몸무게 · 수면(선택) · 인바디(선택) · 동의)
  → 현재 프로필 생성

[02 · 추구미 입력과 결과]
우선순위를 선택할까?
  ├ 예   → 우선순위 1개 선택 (피부 · 얼굴형 · 체형 · 건강)
  └ 아니오 → 우선순위 건너뛰기 (카테고리 동일 가중치)
  → 추구미 입력 방식 선택
     ├ 자연어        → 자연어 키워드 추출 (문장 속 분위기 · 목표 분석)
     └ 자연어 + 사진 → 자연어·사진 키워드 추출 (공통 · 충돌 키워드 구분)
  → 키워드 1개 이상 선택 (사용자가 AI 후보를 직접 확정)
  → 추구미 결과 확인 (예상 이미지 · 변화 설명 · 관리 방향)
  → 결과가 마음에 드는가?
     ├ 아니오 → 추구미 다시 입력 (로그인·프로필은 유지) → 입력 방식 선택으로 회귀
     └ 예     → 결과 저장 (즐겨찾기 정보 · 갤러리 이미지)

[03 · 맞춤 루틴과 지속 관리]
메인 화면 · 루틴 생성
  → 카테고리 선택 (피부 · 건강 · 체형)
  → 카테고리별 기간 선택 (각 1~12주 · 권장 기간 제공)
  → 루틴 생성 결과
  → 루틴 화면 · 완료 체크
     └ 미수행 시 → 다음 가능한 날짜로 유동적 재배치 → 루틴 화면으로 회귀
```

**플로우 상 반드시 지켜야 할 규칙**

- R-1. 추구미 재입력 시 **로그인 상태와 현재 프로필은 유지**한다. (재촬영·재등록 요구 금지)
- R-2. 우선순위를 건너뛰면 모든 카테고리에 **동일 가중치**를 적용한다.
- R-3. 키워드는 **최소 1개 필수 선택**이며, 선택 전에는 결과 생성 버튼이 비활성이다.
- R-4. 결과 저장은 "마음에 들어요" 분기에서만 발생한다. (자동 저장 없음)
- R-5. 루틴 생성은 **결과 저장 이후 메인 화면**에서 진입한다.

---

## 6. 기능 요구사항

우선순위: **P0** = MVP 필수 / **P1** = MVP 권장 / **P2** = 이후 확장

### F-01. 로그인 · 회원가입 (P0)

| 항목 | 내용 |
| --- | --- |
| 설명 | 이메일 또는 소셜 로그인으로 계정을 생성/인증한다. |
| 요구사항 | 분석 결과·루틴은 계정에 귀속되므로 **분석 전 로그인 필수**. 비로그인 사용자는 랜딩까지만 접근 가능. |
| 수용 기준 | ① 로그인 성공 시 메인 화면으로 이동한다. ② 세션 만료 시 재로그인 후 직전 진입 지점으로 복귀한다. ③ 로그아웃 후에도 저장된 결과/루틴은 유지된다. |

### F-02. 내 사진 · 기본 정보 등록 (P0)

| 필드 | 필수 | 형식 / 제약 |
| --- | --- | --- |
| 상반신 사진 | 필수 | JPG/PNG/HEIC, 최대 10MB, 얼굴 1인 검출 필수 |
| 생년월일 | 필수 | YYYY-MM-DD, 만 14세 이상 |
| 성별 | 필수 | 남성 / 여성 / 선택 안 함 |
| 키 | 필수 | 100~250cm, 정수 |
| 몸무게 | 필수 | 30~200kg, 소수 1자리 |
| 평균 수면 시간 | 선택 | 0~14시간, 0.5시간 단위 |
| 인바디 정보 | 선택 | 골격근량 / 체지방률 / BMI (직접 입력) |
| 개인정보 활용 동의 | 필수 | 필수 동의 · 선택 동의 분리 |

**사진 검증 규칙**

- MediaPipe Face Detector로 얼굴 검출. 미검출 / 2인 이상 / 과도한 블러·저조도 시 재업로드 안내.
- 검증 실패 사유는 **구체 문구**로 안내한다. (예: "얼굴이 인식되지 않았어요. 정면을 향한 밝은 사진을 올려주세요.")

**수용 기준** — ① 필수 항목 미입력 시 제출 불가. ② 필수 동의 미체크 시 제출 불가. ③ 제출 성공 시 `현재 프로필`이 생성되고 프로필 카드가 노출된다.

### F-03. 현재 프로필 생성 (P0)

- 업로드된 사진과 신체 정보를 AI가 분석해 **현재 상태 요약**(얼굴 인상 특징, 체형 구간, 건강 참고 지표)을 구조화 저장한다.
- 프로필은 재사용 자산이다. 추구미 재입력·추가 분석 시 재생성하지 않는다.
- **수치 점수화 금지.** "피부 점수 72점" 같은 표현을 출력하지 않는다. (서비스 원칙 §1.5)

### F-04. 우선순위 선택 (P0)

- 선택지: **피부 · 얼굴형 · 체형 · 건강** 중 **1개**.
- "건너뛰기" 제공. 건너뛰면 4개 카테고리 **동일 가중치**.
- 선택된 카테고리는 결과의 변화 Top 3 정렬과 루틴 우선순위(우선 1 / 우선 2)에 가중치로 반영된다.

### F-05. 추구미 입력 (P0)

| 입력 방식 | 필수 요소 | AI 처리 |
| --- | --- | --- |
| 자연어 | 텍스트 (10자 이상, 300자 이내) | 문장 속 분위기·목표 분석 |
| 자연어 + 사진 | 텍스트 + 참고 이미지 1장 | **공통 키워드 / 충돌 키워드** 구분 |

- **텍스트는 두 방식 모두 필수**다. 사진만 등록하는 경로는 제공하지 않는다.
- 충돌 키워드: 텍스트가 지향하는 방향과 사진이 지향하는 방향이 다를 때 별도 그룹으로 표시하고, 사용자가 어느 쪽을 택할지 선택하게 한다.
- 참고 이미지는 **분위기 요소 추출 목적**으로만 사용하며, 타인의 얼굴을 복제·합성 대상으로 삼지 않는다.

### F-06. 키워드 추출 및 선택 (P0)

- AI가 키워드 후보를 **5~8개** 제안한다. 각 키워드는 `라벨 + 근거 한 줄 + 카테고리(피부/얼굴형/체형/건강)`를 가진다.
- 사용자는 **1개 이상** 선택한다. 상한은 4개.
- **최종 확정은 사용자**가 한다. AI 후보는 기본 선택 상태가 아니다. (서비스 원칙 §1.5)

### F-07. 추구미 결과 화면 (P0) — 핵심 화면

첨부된 결과 화면 시안을 정본으로 하며, 아래 블록 순서를 고정한다.

| # | 블록 | 구성 요소 | 비고 |
| --- | --- | --- | --- |
| 1 | 헤더 | `AI 분석 완료` 배지 + 분석일자 | |
| 2 | 타이틀 | "{닉네임}님의 추구미가 완성됐어요" + 한 줄 요약 | |
| 3 | 비교 이미지 | 좌 `현재` / 우 `추구미` 분할 이미지 | 캡션: "좌우 비교해 본 모습을 기준으로 한 임시 이미지예요." |
| 4 | 한눈에 보는 추구미 | 선택 키워드 칩 / **유지할 점** / **강조할 점** / **변화 강도**(예: 자연스럽게 · 35%) | 사용자가 선택한 키워드 중심으로 정리 |
| 5 | 이렇게 바꾸면 가까워져요 | **변화 Top 3** (번호 · 제목 · 설명 · 태그[스타일링/셀프 관리/케어]) | 우선순위 가중치 반영 정렬 |
| 6 | 오늘부터 해볼 관리 | 즉시 실행 가능한 관리 3개 | 큰 변화보다 일상에서 시작 가능한 항목 우선 |
| 7 | 추천 맞춤 루틴 | 카테고리별 카드(추천 기간 · 하루 소요 · 주당 횟수 · `우선 1`/`우선 2`/`선택` 뱃지) + **`내 루틴 만들기`** CTA | 최대 3개 선택, 기간 변경 가능 |
| 8 | 결과 피드백 | "이 결과가 마음에 드나요?" → **`마음에 들어요`** / **`다시 분석하기`** | §5 R-4 분기 |
| 9 | 면책 문구 | "AI가 생성한 참고용 이미지와 관리 방향입니다. 피부·건강 상태에 대한 의료적 진단이나 시술 결과를 의미하지 않습니다." | 상시 노출, 숨김 불가 |

**이미지 출력 규칙**

- 참고 이미지를 첨부한 경우: 사용자 사진 기반의 예상 이미지를 생성해 **좌우 비교 형태**로 제공한다.
- 텍스트만 입력한 경우: 이미지 블록을 생략하고 **텍스트 결과만** 제공한다. (레이아웃이 깨지지 않도록 대체 요약 카드 노출)

**수용 기준** — ① `다시 분석하기` 선택 시 §5 R-1에 따라 추구미 입력 방식 선택 단계로 회귀한다. ② `마음에 들어요` 선택 시 F-08이 실행된다. ③ 면책 문구가 없는 결과 화면은 배포 불가.

### F-08. 결과 저장 (P0)

- 저장 대상: **즐겨찾기 정보(구조화 결과 JSON)** + **갤러리 이미지(비교 이미지 PNG)**.
- 저장된 결과는 `서랍`에서 목록으로 열람하며, 루틴 생성 시 "저장된 분석 결과 가져오기"의 소스가 된다.
- 이미지 다운로드는 디바이스 갤러리 저장 권한을 요청한 뒤 수행한다.

### F-09. 루틴 생성 (P0)

진입 경로 2가지:

| 경로 | 동작 |
| --- | --- |
| 저장된 분석 결과 가져오기 | 선택한 분석 결과의 키워드·변화 Top 3를 근거로 루틴을 생성 |
| 루틴만 생성 | 분석 결과 없이 카테고리 선택부터 시작 |

- 카테고리: **피부 · 건강 · 체형** (복수 선택, 최대 3개)
- 기간: 카테고리별 **1~12주** 내에서 사용자가 선택. 각 카테고리에 **권장 기간**을 기본값으로 제시한다.
  - 권장 기본값: 피부 4주 · 건강 3주 · 체형 2주 (결과 화면 시안 기준, 튜닝 가능)
- 출력: 루틴명, 주당 수행 횟수, 하루 예상 소요 시간, 일자별 태스크 목록.

### F-10. 루틴 수행 · 완료 체크 · 재배치 (P0)

- 루틴 화면에서 태스크 단위로 완료 체크한다.
- **미수행 루틴은 다음 가능한 날짜로 유동적 재배치**한다.
  - 재배치 규칙: 미수행 태스크는 익일 이후 가장 가까운 빈 슬롯으로 이동. 동일 주 내 재배치 실패 시 다음 주로 이월하며, 전체 기간은 연장하지 않는다.
  - 연속 3회 미수행 시 "강도 낮추기" 제안 카드를 노출한다.
- 사용자를 **비난하거나 실패로 규정하는 문구를 쓰지 않는다.** (서비스 원칙 §1.5)

### F-11. 알림 (P1)

- 루틴 태스크 시간에 맞춰 푸시/웹 알림을 발송한다.
- 사용자는 알림 시각과 on/off를 설정할 수 있다. 기본은 off이며 최초 루틴 생성 시 동의를 받는다.

### F-12. 구독 · 결제 게이팅 (P1)

- 앱은 무료, 기능은 유료. **한 달 무료 체험** 제공.
- 무료 **분석권 1회** 제공. 소진 후 분석 시도 시 구독 안내 모달.
- 요금제: **월 구독 8,900원 / 연 구독 89,000원** (전 기능 이용)
- 결제 실패·해지 시 저장된 결과와 루틴은 **열람 가능**, 신규 분석·루틴 생성은 차단한다.

---

## 7. 데이터 모델

> **정본은 [ERD.md](ERD.md)** 다. 아래는 개념 요약이며, 컬럼·타입·제약·인덱스·DDL은 ERD 문서를 따른다.
> API 계약의 정본은 **[API.md](API.md)** 다.

```text
User            id, email, provider, nickname, createdAt, subscriptionId
Consent         id, userId, type(REQUIRED|OPTIONAL), version, agreedAt
Profile         id, userId, photoUrl, birthDate, gender, heightCm, weightKg,
                sleepHours?, inbody?{skeletalMuscle, bodyFatRate, bmi},
                analysisSummary(JSON), createdAt
Analysis        id, userId, profileId, priorityCategory?(SKIN|FACE|BODY|HEALTH|NULL),
                inputMode(TEXT|TEXT_IMAGE), inputText, referenceImageUrl?,
                status(PENDING|DONE|FAILED), createdAt
Keyword         id, analysisId, label, reason, category,
                origin(TEXT|IMAGE|COMMON|CONFLICT), selected(boolean)
AnalysisResult  id, analysisId, comparisonImageUrl?, summary,
                keepPoints[], emphasizePoints[], changeIntensity(0~100),
                topChanges[3]{rank, title, description, tag},
                dailyCares[], recommendedRoutines[], likedAt?
SavedResult     id, userId, analysisResultId, savedAt
Routine         id, userId, analysisResultId?, category(SKIN|HEALTH|BODY),
                durationWeeks(1~12), perWeek, minutesPerDay, startDate, status
RoutineTask     id, routineId, scheduledDate, title, description,
                status(PENDING|DONE|MISSED), rescheduledFrom?
Subscription    id, userId, plan(TRIAL|MONTHLY|YEARLY), analysisCredits,
                startedAt, expiresAt, status
```

### API

엔드포인트 33종의 요청/응답 스펙, Enum, 에러 코드는 [API.md](API.md)에 정의되어 있다.

---

## 8. AI 요구사항

### 8.1 파이프라인

```text
[1] 프로필 분석    입력: 사용자 사진 + 신체/건강 정보
                   출력: 현재 상태 구조화 요약 (얼굴 인상 요소, 체형 구간, 참고 지표)

[2] 키워드 추출    입력: 추구미 텍스트 (+ 참고 이미지)
                   출력: 키워드 후보 5~8개
                        (라벨 / 근거 / 카테고리 / origin: TEXT·IMAGE·COMMON·CONFLICT)

[3] 결과 생성      입력: [1] + 사용자가 선택한 키워드 + 우선순위 가중치
                   출력: 유지할 점 / 강조할 점 / 변화 강도 / 변화 Top 3 /
                        오늘부터 해볼 관리 / 추천 루틴 후보

[4] 이미지 생성    입력: 사용자 사진 + 선택 키워드 (참고 이미지 첨부 시에만 수행)
                   출력: 좌우 비교 이미지

[5] 루틴 생성      입력: [3] 결과 + 카테고리 + 기간
                   출력: 주차별·일자별 태스크
```

- 모든 텍스트 단계는 **Structured Outputs(JSON Schema)** 로 강제한다. 자유 서술 파싱 금지.
- 이미지 생성은 실패 가능성이 높으므로 **텍스트 결과와 분리된 비동기 처리**로 구성하고, 실패 시 텍스트 결과만으로 화면이 성립해야 한다.

### 8.2 AI 가드레일 (필수)

| # | 규칙 |
| --- | --- |
| G-1 | 외모를 점수·등급·순위로 평가하지 않는다. (`72점`, `상위 20%` 등 금지) |
| G-2 | 참고 이미지 속 **타인의 얼굴을 복제하지 않는다.** 분위기 요소(라인·톤·볼륨)만 적용한다. |
| G-3 | 의료 진단·시술 권유·효능 보장 표현을 하지 않는다. (`~하면 치료된다`, `시술을 받으세요` 금지) |
| G-4 | 체중 감량 목표를 수치로 단정하거나 극단적 식이·단식을 제안하지 않는다. |
| G-5 | 사용자가 입력하지 않은 정보를 근거로 삼지 않는다. 근거 문장에 어떤 입력을 참조했는지 명시한다. |
| G-6 | 결과 문구는 사용자를 결점 중심으로 서술하지 않는다. "부족하다" 대신 "이렇게 하면 가까워진다"로 표현한다. |
| G-7 | 미성년자(만 14세 미만) 및 얼굴 미검출 이미지는 분석하지 않는다. |

### 8.3 실패 처리

| 상황 | 처리 |
| --- | --- |
| 얼굴 미검출 / 다인 검출 | 업로드 단계에서 차단 + 재업로드 안내 |
| 키워드 추출 0건 | "조금 더 구체적으로 적어주세요" 안내 + 예시 문장 3개 제시 |
| 이미지 생성 실패 | 텍스트 결과만 노출 + "예상 이미지는 생성하지 못했어요" 안내, 분석권 차감 취소 |
| 전체 분석 타임아웃(60초) | 실패 처리 + 분석권 미차감 + 재시도 버튼 |

---

## 9. 비기능 요구사항

| 구분 | 요구사항 |
| --- | --- |
| 성능 | 분석 요청→결과 노출 P95 30초. 30초 초과 시 진행 상태 애니메이션과 예상 시간 표시. |
| 반응형 | Mobile First(360px 기준). 태블릿·데스크톱은 중앙 정렬 반응형으로 대응. |
| 접근성 | 본문 대비비 4.5:1 이상, 터치 타겟 44×44px 이상, 이미지 대체 텍스트 제공. |
| 보안 | 사진·건강정보는 전송/저장 구간 암호화. 이미지 URL은 서명된 만료 URL 사용. |
| 로깅 | 분석 요청·결과·실패를 사용자 식별자와 분리해 적재. 원본 사진은 분석 로그에 저장하지 않는다. |
| 브라우저 | 최신 Chrome / Safari / Samsung Internet 2개 메이저 버전. |

---

## 10. 개인정보 · 법적 고지

- 수집 항목: 사진(생체정보에 준함), 생년월일, 성별, 키, 몸무게, 수면 시간(선택), 인바디(선택).
- **필수 동의와 선택 동의를 분리**하고, 선택 동의를 거부해도 핵심 기능을 이용할 수 있어야 한다.
- 보관 기간 및 파기 정책을 동의서에 명시하고, 마이페이지에서 **사진 삭제 / 계정 삭제**를 제공한다.
- 만 14세 미만 가입 불가.
- 모든 결과 화면과 루틴 화면에 **의료적 진단이 아님을 알리는 면책 문구**를 상시 노출한다.

---

## 11. 비즈니스 모델

| 항목 | 내용 |
| --- | --- |
| 구조 | 앱 무료 · 기능 유료 |
| 무료 제공 | 한 달 무료 체험, 기본 분석권 1회 |
| 월 구독 | 8,900원 — 전 기능 이용 |
| 연 구독 | 89,000원 |
| 데이터 가치 | 사용자가 제공하는 정보의 축적 자체가 서비스 개선 자산 (단, §10 동의 범위 내에서만 활용) |

---

## 12. 기술 구성 (예정)

| 영역 | 기술 |
| --- | --- |
| Frontend | React · TypeScript · Mobile First |
| Backend | Spring Boot 3.x · Spring Data JPA · REST API |
| Database | **PostgreSQL 16** (관계 구조 + JSONB) · Flyway 마이그레이션 |
| Storage | S3 호환 오브젝트 스토리지 (**국내 리전**) · presigned URL |
| AI | **OpenAI API** — 텍스트 단계는 Structured Outputs(strict JSON Schema), 이미지 단계는 image edits |
| Image Validation | MediaPipe Face Detector (프론트 1차) + 서버 2차 검증 |

- 모델 ID는 `application.yml`에 핀 고정하고 실제 사용값을 `ai_jobs.model`에 기록한다. ([API.md](API.md) §7.1)
- **OpenAI API 키는 서버 환경변수로만 관리하며, 프론트에서 직접 호출하지 않는다.**
- Redis·벡터DB는 MVP 범위에서 제외한다. 비동기 분석 상태는 `analyses.status` 폴링으로 처리한다.

---

## 13. 마일스톤 (해커톤 기준)

| 단계 | 산출물 |
| --- | --- |
| M1 | 유저플로우·화면 정의 확정, API 스펙 고정, AI 출력 JSON Schema 확정 |
| M2 | 로그인 + 프로필 등록 + 사진 검증 동작 |
| M3 | 키워드 추출 → 선택 → 텍스트 결과 생성 (E2E) |
| M4 | 비교 이미지 생성 + 결과 저장 + 서랍 |
| M5 | 루틴 생성 · 완료 체크 · 재배치 |
| M6 | 알림, 구독 게이팅, 가드레일 검수, 시연 리허설 |

**데모 필수 경로**: 로그인 → 프로필 등록 → 우선순위 선택 → 자연어+사진 입력 → 키워드 선택 → 결과 확인 → 저장 → 루틴 생성 → 완료 체크

---

## 14. 미결 사항 (Open Issues)

개발 착수 전 확정 필요. 현재는 **오른쪽 "본 문서 기준"** 값으로 구현한다.

| # | 쟁점 | 근거 자료 간 차이 | 본 문서 기준 |
| --- | --- | --- | --- |
| O-1 | 우선순위 카테고리 | 기획 텍스트는 `피부/체형/건강` 3종, 유저플로우 다이어그램은 `피부/얼굴형/체형/건강` 4종 | **4종** (다이어그램이 최신) |
| O-2 | 루틴 기간 | 기획 텍스트는 `체형 최소 1개월 / 피부 최소 3개월 / 건강 자유`, 다이어그램은 `각 1~12주` | **각 1~12주 + 권장 기간 제시** |
| O-3 | 로그인 필수 여부 | README는 "로그인 없이 결과 확인", 유저플로우는 로그인 필수 | **로그인 필수** (결과 저장·루틴이 계정 종속) |
| O-4 | 루틴 카테고리 | 우선순위는 4종이나 루틴 카테고리는 `피부/건강/체형` 3종 | 루틴은 **3종 유지** (얼굴형은 결과의 변화 Top 3로만 반영) |
| O-5 | 변화 강도 산출식 | 시안에 `자연스럽게 · 35%` 표기 | 산출 로직 미정 — M1까지 정의 필요 |
| O-6 | 분석권 차감 시점 | 미정 | **결과 생성 성공 시점**에 차감, 실패 시 미차감 |

---

<div align="center">
  <sub>AI 분석과 생성 이미지는 스타일 탐색을 위한 참고용이며, 실제 결과를 보장하거나 의료적 진단을 제공하지 않습니다.</sub>
</div>
