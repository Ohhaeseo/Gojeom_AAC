# GO. (고점) — Product Requirements Document

| 항목 | 내용 |
| --- | --- |
| 프로젝트명 | GO. (고점) |
| 문서 버전 | v2.0 |
| 최종 수정일 | 2026-08-13 |
| 소속 | 멋쟁이사자처럼 대학 14기 중앙 해커톤 · 주제기업 AAC(Anti-Aging Club) · 팀 뚝딱이들 |
| 플랫폼 | Mobile Web (Mobile First) |
| 문서 목적 | 기획·디자인·프론트·백엔드·AI가 공유하는 **개발 기준 문서** |

> **v2 변경 요약** — [Figma 시안](https://www.figma.com/design/8AX19ImZG4ou6jqCwU0tPJ/GO.) 41장과 대조해 시안 기준으로 재작성했다. 주요 변경: 사용자 대면 용어를 **"추구미" → "고점"** 으로 통일, 우선순위를 **3종 순위 지정 방식**으로 확정, 우선순위를 분석이 아닌 **프로필 속성**으로 이동, 인바디 6종 확장, 서류 스캔 기능 추가.

**문서 체계**

| 문서 | 정본 범위 |
| --- | --- |
| PRD.md (본 문서) | 제품 요구사항, 기능 정의, 플로우 |
| [design.md](design.md) | 디자인 토큰, 컴포넌트, 화면 레이아웃 |
| [API.md](API.md) | Frontend ⇄ Backend API 계약 |
| [ERD.md](ERD.md) | 데이터 모델, 스키마 |

---

## 1. 제품 개요

### 1.1 한 줄 정의

> **GO.** 은 AI가 사용자의 현재 얼굴과 신체정보를 분석하고, 가장 현실적으로 도달 가능한 **고점**과 변화 전략을 제안하여 사용자의 **고점(Highest Potential)** 으로 나아가도록 돕는 AI 기반 이미지 전략 서비스다.

### 1.2 용어

| 용어 | 정의 | 비고 |
| --- | --- | --- |
| **고점** | 사용자가 도달하고자 하는 이상적인 자기 이미지 | **사용자 대면 용어.** 기획 초안의 "추구미"를 대체한다 |
| 고점 분석 | 현재 프로필과 고점 입력을 비교 분석하는 과정 | 코드/DB에서는 `analysis` |
| 서랍 | 저장한 분석 결과 보관함 | |
| 목표 | 분석 결과를 바탕으로 생성된 실행 계획 | 코드/DB에서는 `routine` |

### 1.3 목표

입력한 내 얼굴과 고점(또는 추구하는 생활·체형·건강)을 바탕으로 AI가 비교 분석한 뒤, 나에게 맞는 고점과 **현실적인 변화 전략 + 지속 가능한 목표**를 제안한다.

### 1.4 배경

**① 웰니스 측면 — 수치 관리의 피로**

웰니스는 신체적·정신적·사회적 건강의 균형을 추구하는 활동이다. 그러나 웰니스 실패 요인으로 *"건강을 점수로 기록하고 수치로 관리하는 방식에 대한 피로"* 가 부상하고 있다. 수치는 분석·관리에는 탁월하지만 인간에게 피로감을 주고, 불특정 다수의 빅데이터로 만들어진 **보편화된 기준은 나에게 맞지 않을 수 있다.**

→ GO.는 보편 수치가 아닌 **'고점(이상적인 나)'** 을 목표로 세워, 오직 나만을 위한 기준으로 일상을 바꾼다.

> ⚠️ 이 배경은 현재 시안의 홈 화면(수치 대시보드)과 충돌한다. §14 O-1 참조.

**② 미용 측면 — 채널 분산으로 인한 소비자 혼란**

> 김경인. (2021). 멀티채널환경의 정보탐색과 소비자 혼란에 대한 연구 - 뷰티서비스를 중심으로 -. 한국디자인포럼, 26(1), 147-156.

뷰티 정보 채널(온라인/오프라인/SNS/후기/병원/광고)이 과도하게 분산되어 소비자는 **Consumer Confusion**을 겪는다. 사용자는 하나의 원하는 이미지를 만들기 위해 여러 채널을 반복 탐색하지만, 신뢰 수준이 달라 무엇이 자신에게 맞는지 판단하기 어렵다.

→ GO.는 **하나의 이미지(고점)를 중심으로 모든 솔루션을 연결한다.**

### 1.5 문제 정의와 해결

| 구분 | 내용 |
| --- | --- |
| 사용자 문제 | SNS에서 관리·스타일링 정보를 자주 접하고 원하는 분위기의 레퍼런스를 저장하지만, 자신의 얼굴·체형·생활루틴에 **무엇부터 적용해야 할지 몰라** 탐색과 결정을 반복한다. |
| 해결 방안 | 사용자의 정보(사진·신체·건강정보)와 사용자가 등록한 고점을 AI가 비교 분석해, **현실적으로 도달 가능한 변화 전략**을 제안한다. |
| 핵심 가치 | "원하는 이미지에 **가까워지는 방법**"을 얻는 것. |
| 차별점 | 고점(사진/텍스트)과 사용자 정보를 **상세 비교 분석**하여 적용 가능한 방안을 제안하고, **변화된 모습을 시각적으로 보여주며**, 이를 **목표로 연결**한다. |

### 1.6 서비스 원칙

> 외모를 점수화하거나 타인의 얼굴을 복제하지 않는다.

| 원칙 | 의미 |
| --- | --- |
| 평가보다 해석 | 결점이 아닌 분위기 구성 요소를 설명한다. |
| 복제보다 적용 | 참고 이미지의 **스타일**(헤어·피부 상태·분위기)을 나에게 맞게 적용한다. 그 사람의 **얼굴**이 되지는 않는다. |
| 이미지보다 행동 | 실제로 시도할 수 있는 변화 순서를 제안한다. |
| 사용자 선택 우선 | AI 추천보다 사용자의 최종 선택을 우선한다. |

---

## 2. 타겟 사용자

### 2.1 타겟

SNS에서 관리·스타일링 정보를 자주 접하고, 원하는 분위기의 레퍼런스를 저장하지만 자신의 얼굴·체형·생활루틴에 무엇부터 적용해야 할지 몰라 탐색과 결정을 반복하는 **20~30대 사용자**.

### 2.2 사용자 시나리오 (Happy Path)

1. 사용자가 **상반신 사진**을 등록하고, **피부·체형·건강에 1·2·3순위**를 매긴다.
2. **키·몸무게**를 필수로 입력하고, **평균 수면 시간·인바디 6종**을 선택 입력한다. 인바디는 **서류를 카메라로 스캔**해 자동 입력할 수도 있다.
3. **고점**을 자유 텍스트(최대 500자)로 적고, 참고 사진을 **여러 장** 첨부할 수 있다.
4. 분석이 진행되는 동안 AI가 추출한 **키워드를 1개 이상 선택**한다.
5. **결과 화면**(비교 이미지 · 고점 요약 · 카테고리별 변화 · 오늘 해볼 관리)을 확인한다.
6. 마음에 들면 **서랍에 저장**한다.
7. 서랍에서 결과를 열어 **맞춤형 목표로 설정**하면, AI가 실행 가능한 목표를 생성하고 **알림**을 보내준다.

---

## 3. 성공 지표 (MVP)

| 구분 | 지표 | 목표 |
| --- | --- | --- |
| 핵심 전환 | 프로필 등록 → 고점 분석 완료율 | 60% 이상 |
| 결과 만족 | 결과 화면에서 "서랍에 결과 저장하기" 선택 비율 | 70% 이상 |
| 연결 전환 | 분석 완료 → 목표 설정 전환율 | 40% 이상 |
| 지속성 | 목표 생성 후 7일 내 완료 체크 1회 이상 | 50% 이상 |
| 품질 | 분석 실패(에러·타임아웃) 비율 | 3% 이하 |
| 성능 | 분석 요청 → 결과 노출 P95 | 30초 이내 |

---

## 4. 범위

### 4.1 MVP 포함

- 로그인 / 회원가입
- 사진 + 우선순위 + 신체 정보 등록 → 현재 프로필 생성
- 인바디 서류 카메라 스캔 (OCR 자동 입력)
- 고점 입력 (텍스트 필수 + 참고 사진 다중 선택)
- AI 키워드 추출 및 사용자 선택
- 결과 생성 (비교 이미지 · 고점 요약 · 카테고리별 변화 · 오늘의 관리)
- 서랍 저장 및 3개 섹션 열람
- 목표 생성 **2경로** (저장된 분석 결과 기반 / 새 루틴 만들기), 완료 체크
- 목표 알림
- 프로필 조회·수정, 우선순위 변경, 분석 전체 삭제

### 4.2 MVP 제외

- 소셜/커뮤니티, 공유 피드
- 제품·시술 커머스 연동
- 웨어러블 자동 연동
- 다국어
- 데스크톱 전용 레이아웃

### 4.3 미설계 (시안 없음 · §14 참조)

- 개인정보 동의 화면
- 생년월일·성별 입력
- 구독·결제
- 루틴 탭 경로 선택 · 분석 결과 선택 목록 · 카테고리/기간 선택 (F-09 경로 B)
- 미수행 목표 재배치 UI

---

## 5. 유저플로우

Figma 시안의 화면 번호를 기준으로 한다.

```text
[01 · 진입과 계정]
01 실행 → 02 로그인/회원가입 선택 → 03 로그인 · 03-1 회원가입
   → 04 홈(프로필 무)

[02 · 프로필 생성]
04 홈 → 05 사진등록 → 06 우선 순위 등록 → 07 키·몸무게 등록
   → 08 선택 정보 등록 (수면 · 인바디 6종 · 서류 스캔)
   → 09 정보 입력 완료 → 10 프로필 분석 중 → 11 홈(프로필 유)

[03 · 고점 분석]
목표 설정 탭 → 12 고점 등록 (텍스트 필수 + 사진 다중 선택)
   → 13 고점 분석 중
        └ 14 키워드 선택 (블러 위 오버레이 카드, 최소 1개)
   → 15 고점 분석 → 16 고점 분석 완료 (5초 후 자동 전환)
   → 17 결과지 [FRESH]
        ├ 서랍에 결과 저장하기 → 18 저장 안내 → 홈
        └ 새로 분석하기 → 12로 회귀 (프로필 유지)

[04 · 목표와 지속 관리]
루틴 탭 → 경로 선택
   ├ A. 저장된 분석 결과 가져오기
   │     → 서랍의 결과 목록에서 선택
   │     → 21 목표 설계 중 → 22 목표 설계 완료 → 23 목표
   │     ※ 19 서랍 → 20 결과지[SAVED] → 맞춤형 목표로 설정하기 도 같은 경로
   │
   └ B. 새 루틴 만들기
         → 카테고리 선택 (피부·체형·건강 중 1~3개)
         → 카테고리별 기간 선택 (각 1~12주)
         → 21 목표 설계 중 → 22 목표 설계 완료 → 23 목표

23 목표 → 태스크 완료 체크 · 알림 설정 · 목표 삭제
```

### 반드시 지킬 규칙

| # | 규칙 |
| --- | --- |
| R-1 | "새로 분석하기"는 **로그인 상태와 프로필을 유지**한다. 사진 재등록을 요구하지 않는다. |
| R-2 | 우선순위는 **3종 전부에 순위를 매긴다.** 건너뛰기 없음. |
| R-3 | 키워드는 **최소 1개 필수 선택**. 미선택 시 저장 버튼 비활성. |
| R-4 | 결과 저장은 사용자가 "서랍에 결과 저장하기"를 눌렀을 때만 발생한다. 자동 저장 없음. |
| R-5 | 목표 생성 경로는 **2종**이다. 저장된 분석 결과 기반(A) / 분석 결과 없이 새로 생성(B). 저장된 결과가 없으면 A 진입 시 B로 유도한다. |
| R-6 | 결과 화면에는 **면책 문구를 항상 노출**한다. |

---

## 6. 기능 요구사항

**P0** = MVP 필수 / **P1** = MVP 권장 / **P2** = 이후 확장

### F-01. 로그인 · 회원가입 (P0)

- 이메일 또는 소셜(Google) 로그인.
- 분석 결과·목표가 계정에 귀속되므로 **분석 전 로그인 필수**.
- 세션 만료 시 재로그인 후 직전 진입 지점으로 복귀.
- 가입 시 무료 체험과 분석권이 부여된다. (F-12)

**수용 기준** — ① 로그인 성공 시 홈으로 이동. ② 로그아웃 후에도 저장된 결과·목표는 유지된다.

### F-02. 프로필 등록 (P0)

**한 화면에서 스크롤로 이어지는 단일 폼**이다. (05 → 08)

| 필드 | 필수 | 제약 |
| --- | --- | --- |
| 상반신 사진 | 필수 | JPG/PNG/HEIC, 최대 10MB, 얼굴 1인 검출 |
| **우선순위 1·2·3** | 필수 | 피부 · 체형 · 건강 **3종 전부에 순위 부여** |
| 키 | 필수 | 100~250cm |
| 몸무게 | 필수 | 30~200kg |
| 평균 수면 시간 | 선택 | 0~14시간, 0.5단위 |
| 인바디 (6종) | 선택 | 체수분(L) · 단백질(kg) · 무기질(kg) · 체지방(kg) · 골격근량(kg) · BMI(무단위) |

**v1 대비 변경**

- 우선순위가 **4종 중 1개 선택 → 3종 순위 지정**으로 바뀌었다. `얼굴형`은 카테고리가 아니라 키워드 차원에서 다뤄진다("다이아몬드형" 등).
- 우선순위는 **분석이 아니라 프로필의 속성**이다. 프로필 화면(11)에서 언제든 변경할 수 있다.
- 인바디가 3종 → **6종**으로 확장됐다.
- **BMI는 무단위다.** 시안의 `kg` 표기는 오류이므로 구현 시 제거한다.

**사진 검증** — MediaPipe Face Detector로 프론트에서 1차 검증(얼굴 미검출 / 2인 이상 / 저품질), 서버에서 2차 검증. 실패 사유는 구체 문구로 안내한다.

**수용 기준** — ① 필수 항목 미입력 시 제출 불가. ② 우선순위 3개가 모두 지정되지 않으면 제출 불가. ③ 제출 성공 시 현재 프로필이 생성된다.

### F-03. 인바디 서류 스캔 (P1) 〔신규〕

- "직접 입력하기 어렵다면?" → **카메라로 서류 스켄하기**
- 인바디 결과지를 촬영하면 AI가 6개 항목을 추출해 입력 폼을 자동 채운다.
- **추출값은 사용자가 확인·수정한 뒤 저장한다.** 자동 확정하지 않는다.
- 인식 실패 시 직접 입력으로 유도한다.

### F-04. 현재 프로필 생성 (P0)

- 사진과 신체 정보를 AI가 분석해 **현재 상태 요약**을 구조화 저장한다.
- 프로필은 재사용 자산이다. 재분석 시 재생성하지 않는다.
- **점수화 금지.** "피부 점수 72점" 형태의 출력을 만들지 않는다.

### F-05. 고점 입력 (P0)

| 항목 | 필수 | 제약 |
| --- | --- | --- |
| 고점 텍스트 | **필수** | 10~500자 |
| 참고 사진 | 선택 | **다중 첨부 가능**, 각 10MB 이하 |

**v1 대비 변경** — "입력 방식 선택(텍스트 vs 텍스트+사진)" 분기 화면은 **존재하지 않는다.** 한 화면에서 텍스트를 필수로 받고 사진은 선택으로 첨부한다. 사진 첨부 여부는 결과의 이미지 생성 가능 여부만 결정한다.

- 참고 이미지는 **헤어스타일·피부 상태·분위기**를 가져오는 데 쓴다. 사용자가 닮고 싶어 올린 사진이므로 그 요소들은 합성 대상이다.
- 다만 **타인의 얼굴(이목구비·골격)은 복제·합성하지 않는다.** 결과물은 언제나 사용자 본인으로 보여야 한다.

### F-06. 키워드 추출 및 선택 (P0)

- 분석이 진행되는 동안 **블러 처리된 화면 위 오버레이 카드**로 키워드를 제시한다.
- AI가 키워드 후보 5~8개를 제안하고, 사용자가 **1개 이상** 선택한다. 상한 4개.
- **AI 후보는 기본 선택 상태가 아니다.** 최종 확정은 사용자가 한다.
- 미선택 상태에서는 "키워드 선택 저장하기" 버튼이 비활성이다.

**키워드 정책 (§14 O-5)** — 현재 시안의 키워드 예시("귀족턱", "17호 피부")는 시술 용어·제품 호수에 해당한다. 서비스 원칙 및 G-3(시술 권유 금지)과 충돌할 수 있어 키워드 사전 정책 수립이 필요하다.

### F-07. 결과 화면 (P0) — 핵심 화면

**같은 화면이 두 가지 상태를 갖는다.**

| | `FRESH` (17) | `SAVED` (20) |
| --- | --- | --- |
| 진입 | 목표 설정 → 분석 직후 | 서랍에서 열람 |
| 키워드 칩 | 체크박스형 (조정 가능) | pill형 (확정) |
| CTA | 서랍에 결과 저장하기 / 새로 분석하기 | 맞춤형 목표로 설정하기 |
| 활성 탭 | 홈 | 서랍 |

**블록 순서 (고정)**

| # | 블록 | 구성 |
| --- | --- | --- |
| 1 | 헤더 | 키워드 요약 + 편집 아이콘 + 분석일자 |
| 2 | 비교 이미지 | **좌우 비교 슬라이더** (현재 / 고점), 4:3 가로형 |
| 3 | 한눈에 보는 고점 요약 | 키워드 칩 + 유지할 점 / 강조할 점 / 변화 강도 |
| 4 | 이렇게 바꾸면 가까워져요 | **카테고리(피부·체형·건강)별 1건씩 총 3건** |
| 5 | 오늘 당장 해볼 수 있는 관리 | 제목 + 설명 3건 |
| 6 | 결과가 마음에 든다면? | 상태별 CTA |
| 7 | **면책 문구** | 상시 노출, 숨김 불가 |

**v1 대비 변경**

- 변화 Top 3가 `rank 1·2·3` + `태그(스타일링/셀프관리/케어)` → **카테고리(피부·체형·건강) 기반 3건**으로 바뀌었다.
- `변화 강도`는 퍼센트(35%)가 아니라 **짧은 텍스트**다. 유지할 점·강조할 점과 같은 라벨-값 형식이다.
- **"추천 맞춤 루틴" 카드 블록은 시안에 없다.** 목표 설정은 `SAVED` 상태의 단일 CTA로 진입한다.

**이미지 규칙**

| 상태 | 처리 |
| --- | --- |
| `SKIPPED` (참고 사진 미첨부) | 비교 이미지 블록 미렌더링 |
| `PENDING` | 스켈레톤 유지 + 폴링 계속 |
| `DONE` | 비교 슬라이더 렌더링 |
| `FAILED` | "예상 이미지는 생성하지 못했어요" 안내, 나머지 결과는 정상 노출 |

**수용 기준** — ① 면책 문구가 없는 결과 화면은 배포 불가. ② "새로 분석하기" 선택 시 R-1에 따라 고점 입력 화면으로 회귀한다. ③ `categoryChanges`는 항상 3건(피부·체형·건강 각 1건)이다.

### F-08. 서랍 (P0)

**3개 섹션**으로 구성한다.

| 섹션 | 내용 |
| --- | --- |
| 현재 진행중인 목표 | 목표로 생성되어 진행 중인 분석 |
| 최근 분석 결과 | 한달 내 저장한 고점 분석 결과 |
| 전체 | 그동안 분석한 결과 전부 |

- 저장 대상: 구조화 결과 + 비교 이미지.
- 각 항목은 썸네일·분석일자·요약 제목·진행률로 구성된 리스트 카드다.

### F-09. 목표 생성 (P0) — **경로 2종**

루틴 탭에 진입하면 두 가지 방법 중 하나를 고른다.

| | **A. 저장된 분석 결과 가져오기** | **B. 새 루틴 만들기** |
| --- | --- | --- |
| 근거 | 서랍에 저장된 고점 분석 결과 | 없음 |
| 진입 | 루틴 탭 → `저장된 분석 결과 가져오기` → 결과 선택<br>또는 서랍 → 결과 열람(`SAVED`) → `맞춤형 목표로 설정하기` | 루틴 탭 → `새 루틴 만들기` |
| 사용자 선택 | 어떤 분석 결과를 쓸지 | **카테고리(1~3개) + 카테고리별 기간(1~12주)** |
| 생성 방식 | AI가 결과의 키워드·카테고리별 변화를 근거로 생성 | AI가 선택한 카테고리·기간에 맞춰 생성 |
| 결과 | 여러 카테고리에 걸친 **목표 1개** | **카테고리당 목표 1개** (최대 3개) |

**공통**

- 출력은 태스크 목록이다. 각 태스크는 `제목` / `타이밍 · 소요시간 · 분량` 형식이다.
  - 예: `자외선 차단제 바르기` / `매일 외출 전 / 약 2분 / 4ml`
- 두 경로 모두 우선순위(`profiles.priorities`)를 가중치로 반영한다.

**경로 A 세부**

- 서랍의 저장된 결과 목록에서 하나를 고른다. 저장된 결과가 없으면 **경로 B로 유도**한다.
- 시안 20의 `맞춤형 목표로 설정하기`가 이 경로의 단축 진입이다.

**경로 B 세부**

| 항목 | 규칙 |
| --- | --- |
| 카테고리 | 피부 · 체형 · 건강 중 **1~3개 다중 선택** |
| 기간 | 카테고리별 **1~12주**, 권장 기간을 기본값으로 제시 |
| 권장 기본값 | 피부 4주 · 건강 3주 · 체형 2주 |

**미설계 (§14 O-4)** — 루틴 탭의 경로 선택 화면, 분석 결과 선택 목록, 카테고리·기간 선택 화면이 현재 시안에 없다. [design.md](design.md) §5.6에 필요 화면 구조를 정의해두었으므로 그에 맞춰 디자인이 필요하다.

### F-10. 목표 수행 · 완료 체크 (P0)

- 태스크 단위로 완료 체크한다. 완료 시 카드가 흐려진다.
- 사용자를 비난하거나 실패로 규정하는 문구를 쓰지 않는다.
- 목표 삭제는 확인 모달을 거친다. 모달은 **안전한 선택(되돌아 가기)을 위**, 파괴적 선택(삭제하기)을 아래에 둔다.

**미설계 (§14 O-4)** — 미수행 태스크 재배치 UI가 시안에 없다.

### F-11. 알림 (P1)

- 목표 화면 내 **토글**로 on/off, "알림 설정 상세"에서 시간을 지정한다.
- 기본값은 off이며 최초 목표 생성 시 동의를 받는다.

### F-12. 구독 · 결제 (P1)

- 앱 무료, 기능 유료. **한 달 무료 체험** 제공.
- 무료 **분석권 1회**. 소진 후 분석 시도 시 구독 안내.
- 요금제: **월 8,900원 / 연 89,000원**
- 만료 시 저장된 결과·목표는 **열람 가능**, 신규 분석·목표 생성은 차단.

**미설계 (§14 O-3)** — 관련 화면이 시안에 없다.

### F-13. 프로필 관리 (P0)

- 내 정보 조회 (키·체중·수면·인바디 6종)
- 분석 정보 수정 / 우선 순위 변경
- 내 분석 전체 삭제 (확인 모달)
- 설정 진입 (헤더 우측 gear)

---

## 7. 데이터 모델 · API

정본은 [ERD.md](ERD.md) / [API.md](API.md) 다.

---

## 8. AI 요구사항

### 8.1 파이프라인

```text
[1] PROFILE_ANALYSIS   사진 + 신체/인바디 정보 → 현재 상태 구조화 요약
[2] INBODY_OCR         인바디 서류 촬영본 → 6개 항목 추출          〔신규〕
[3] KEYWORD_EXTRACTION 고점 텍스트 (+ 참고 사진 N장) → 키워드 5~8개
[4] RESULT_GENERATION  [1] + 선택 키워드 + 우선순위 가중치
                       → 요약 / 유지할 점 / 강조할 점 / 변화 강도 /
                         카테고리별 변화 3건 / 오늘의 관리 3건
[5] IMAGE_GENERATION   사용자 사진 + 참고 사진 → 비교 이미지 (참고 사진 있을 때만)
[6] ROUTINE_GENERATION [4] 결과 → 태스크 목록
```

- 모든 텍스트 단계는 **Structured Outputs(strict JSON Schema)** 로 강제한다.
- 이미지 생성은 텍스트 결과와 **분리된 비동기 처리**다. 실패해도 텍스트 결과만으로 화면이 성립해야 한다.
- **우선순위 가중치** — 1순위 카테고리의 변화 제안을 가장 구체적으로, 3순위는 간결하게 생성한다.

### 8.2 AI 가드레일

| # | 규칙 |
| --- | --- |
| G-1 | 외모를 점수·등급·순위로 평가하지 않는다. |
| G-2 | 참고 이미지 속 **타인의 얼굴(이목구비·골격)을 복제하지 않는다.** 사용자 본인의 얼굴을 기준으로 두고, 참고 이미지에서는 **헤어스타일과 피부 상태**를 가져와 합성한다. **피부색은 바꾸지 않는다** — 상태(결·균일함·생기)만 옮긴다. 경계는 아래 «G-2 합성 경계» 표 참조 |
| G-3 | 의료 진단·시술 권유·효능 보장 표현을 하지 않는다. |
| G-4 | 체중 목표를 수치로 단정하거나 극단적 식이·단식을 제안하지 않는다. |
| G-5 | 사용자가 입력하지 않은 정보를 근거로 삼지 않는다. 근거에 참조한 입력을 명시한다. |
| G-6 | 결점 중심으로 서술하지 않는다. "부족하다" 대신 "이렇게 하면 가까워진다"로 표현한다. |
| G-7 | 만 14세 미만 및 얼굴 미검출 이미지는 분석하지 않는다. |
| G-8 | **인바디 OCR 결과를 확정값으로 저장하지 않는다.** 사용자 확인을 거친다. 〔신규〕 |

**프롬프트만으로 보장하지 않는다.** 서버가 출력 텍스트를 후검증한다. ([API.md](API.md) §7.4)

**G-2 합성 경계** 〔2026-08-14 확정〕 — 비교 이미지는 사용자 사진을 기준으로 한 **합성 결과물**이다.

| 참고 이미지에서 가져온다 | 사용자 사진에서 지킨다 |
| --- | --- |
| 헤어스타일 (길이·형태·앞머리·컬·색) | 이목구비의 형태와 배치, 얼굴 골격 |
| 피부 **상태** (결·균일함·생기) | 나이 · 인종 · 성별 · 체형 · **고유 피부색** |
| — | **옷 · 액세서리 · 배경** 〔2026-08-19 추가〕 |
| 전체 분위기와 색감 | |

> **옷을 갈아입히지 않는다** 〔2026-08-19〕. 이 화면이 답하는 질문은 "내가 저 머리와 저 피부라면 어떨까"이지 "내가 저 옷을 입으면 어떨까"가 아니다. 옷까지 바뀌면 사용자가 **무엇이 달라진 것인지 짚어낼 수 없고**, 변화의 원인을 헤어가 아니라 옷으로 읽는다. 액세서리와 배경도 같은 이유로 고정한다.

G-2가 막는 것은 **"그 사람이 누구인지를 결정하는 부분"**이다. 헤어스타일은 정체성이 아니라 바꿀 수 있는 요소이고, 애초에 사용자가 그걸 참고하려고 올린 사진이다.

> **피부는 상태만 옮기고 피부색은 바꾸지 않는다.** 피부색은 인종과 얽혀 있어 "나이·인종·성별을 바꾸지 않는다"와 충돌하고, 밝게·어둡게 바꾸는 기능은 미용 서비스가 넘지 말아야 할 선이다. 이 제품이 다루는 피부도 색이 아니라 상태다(F-02의 SKIN, F-07의 피부 제안 모두 수분·결·톤의 균일함을 말한다).

### 8.3 실패 처리

| 상황 | 처리 |
| --- | --- |
| 얼굴 미검출 / 다인 검출 | 업로드 단계 차단 + 재업로드 안내 |
| 인바디 OCR 실패 | 직접 입력으로 유도, 분석권 미차감 |
| 키워드 추출 0건 | "조금 더 구체적으로 적어주세요" + 예시 문장 제시 |
| 이미지 생성 실패 | 텍스트 결과만 노출 + 안내. **분석권은 차감**(텍스트 결과는 정상 생성됨) |
| 전체 타임아웃(60초) | 실패 처리 + **분석권 미차감** + 재시도 |

---

## 9. 비기능 요구사항

| 구분 | 요구사항 |
| --- | --- |
| 성능 | 분석 요청→결과 노출 P95 30초. 초과 시 진행 상태 표시 |
| 반응형 | **기준 폭 458px** (Figma 프레임). 화면은 세로 스크롤된다 |
| 접근성 | 본문 대비비 4.5:1 이상, 터치 타겟 44×44px 이상, 최소 텍스트 12px |
| 보안 | 사진·건강정보 전송/저장 암호화. 이미지 URL은 서명된 만료 URL |
| 로깅 | 분석 요청·결과·실패를 적재. **원본 사진과 프롬프트 원문은 저장하지 않는다** |
| 브라우저 | 최신 Chrome / Safari / Samsung Internet 2개 메이저 버전 |

> ⚠️ 현재 디자인 토큰의 `primary` 버튼(흰 텍스트 / 민트 배경)은 대비비 **2.22:1** 로 위 기준에 미달한다. [design.md](design.md) §10 참조.

---

## 10. 개인정보 · 법적 고지

- 수집 항목: 사진(생체정보에 준함), 키, 몸무게, 수면 시간(선택), 인바디 6종(선택).
- **필수 동의와 선택 동의를 분리**하고, 선택 동의 거부 시에도 핵심 기능을 이용할 수 있어야 한다.
- 마이페이지에서 **사진 삭제 / 분석 전체 삭제 / 계정 삭제**를 제공한다.
- 만 14세 미만 가입 불가.
- 결과·목표 화면에 **의료적 진단이 아님을 알리는 면책 문구**를 상시 노출한다.
- 오브젝트 스토리지는 **국내 리전**을 사용한다.

> ⚠️ **동의 화면과 생년월일 입력이 현재 시안에 없다.** 만 14세 확인과 생체정보 동의는 법적 요구사항이므로 설계가 필요하다. §14 O-2.

---

## 11. 비즈니스 모델

| 항목 | 내용 |
| --- | --- |
| 구조 | 앱 무료 · 기능 유료 |
| 무료 제공 | 한 달 무료 체험, 기본 분석권 1회 |
| 월 구독 | 8,900원 |
| 연 구독 | 89,000원 |

---

## 12. 기술 구성

| 영역 | 기술 |
| --- | --- |
| Frontend | React · TypeScript · Mobile First |
| Backend | Spring Boot 3.x · Spring Data JPA · REST API |
| Database | PostgreSQL 16 (관계 구조 + JSONB) · Flyway |
| Storage | S3 호환 오브젝트 스토리지 (국내 리전) · presigned URL |
| AI | **OpenAI API** — 텍스트는 Structured Outputs, 이미지는 image edits |
| Image Validation | MediaPipe Face Detector (프론트 1차) + 서버 2차 |

- 모델 ID는 `application.yml`에 핀 고정하고 실제 사용값을 `ai_jobs.model`에 기록한다.
- **OpenAI API 키는 서버 환경변수로만 관리한다.**
- Redis·벡터DB는 MVP 제외. 비동기 상태는 `analyses.status` 폴링으로 처리한다.

---

## 13. 마일스톤

| 단계 | 산출물 |
| --- | --- |
| M1 | 화면 정의 확정, API 스펙 고정, AI 출력 JSON Schema 확정, **§14 O-1 정책 결정** |
| M2 | 로그인 + 프로필 등록(사진·우선순위·신체정보) 동작 |
| M3 | 고점 입력 → 키워드 선택 → 텍스트 결과 생성 (E2E) |
| M4 | 비교 이미지 생성 + 서랍 저장·열람 |
| M5 | 목표 생성 **2경로**(분석 결과 기반 / 새 루틴) · 완료 체크 · 알림 |
| M6 | 인바디 OCR, 구독 게이팅, 가드레일 검수, 시연 리허설 |

**데모 필수 경로**: 로그인 → 프로필 등록 → 고점 입력 → 키워드 선택 → 결과 확인 → 서랍 저장 → 서랍에서 열람 → 목표 설정 → 완료 체크

---

## 14. 미결 사항

| # | 쟁점 | 현재 상태 | 우선순위 |
| --- | --- | --- | --- |
| **O-1** | **수치 평가 UI 정책** — 홈의 "피부 상태 82%/21%…" 및 좋음/나쁨 색 구분이 G-1 및 §1.4 배경과 충돌 | 미결. [design.md](design.md) §11에 선택지 3안 정리 | **최우선** |
| **O-2** | **동의 화면·생년월일·성별 미설계** — 만 14세 확인, 생체정보 동의 | 미설계. 법적 요구사항 | **높음** |
| O-3 | 구독·결제 화면 미설계 | 미설계 | 중간 |
| O-4 | **목표 생성 2경로 화면 미설계** — 루틴 탭 경로 선택, 분석 결과 선택 목록, 카테고리·기간 선택 | 요구사항은 F-09에 확정. [design.md](design.md) §5.6에 구조 정의. 시안 필요 | 높음 |
| O-9 | 미수행 목표 재배치 UI 미설계 | `MISSED` 상태만 정의 | 중간 |
| O-5 | 키워드 정책 — "귀족턱"(시술 용어), "17호 피부"(제품 호수) | 미결. G-3 충돌 소지 | 중간 |
| O-6 | `primary` 버튼 텍스트 대비비 2.22:1 | [design.md](design.md) §10에 대안 제시 | 높음 |
| O-7 | 분석권 차감 시점 | **결과 생성 성공 시점**에 차감, 실패 시 미차감으로 확정 | 확정 |
| O-8 | 소셜 로그인 제공자 | 시안 기준 **Google** (프로필 화면 "Google 로그인" 표기) | 확정 |

---

<div align="center">
  <sub>AI 분석과 생성 이미지는 스타일 탐색을 위한 참고용이며, 실제 결과를 보장하거나 의료적 진단을 제공하지 않습니다.</sub>
</div>
