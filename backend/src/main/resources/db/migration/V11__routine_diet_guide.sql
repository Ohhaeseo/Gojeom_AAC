-- 목표에 딸린 식사 방향.
--
-- **일반 가이드만 담는다.** 〔2026-08-17 팀 결정〕 "탄수화물을 조금 줄여보세요"
-- 수준이고, 끼니별 식단표나 칼로리 지정은 넣지 않는다. 알레르기·질환·복용 약을
-- 모르는 상태에서 구체적인 식단을 지정하면 회의 안건 3번(의료 관련)과 같은
-- 성격의 위험이 생긴다. 프롬프트가 그 경계를 지킨다.
--
-- NULL을 허용한다. 식사와 관련이 옅은 목표(피부 루틴 일부)에는 억지로 붙이지
-- 않는다. 억지로 채우면 "물을 마시세요" 같은 빈 말이 남는다.
--
-- 길이는 스키마의 maxLength 160에 맞춘다. 카드 아래 두세 줄로 그려진다.

ALTER TABLE routines ADD COLUMN diet_guide VARCHAR(160);
